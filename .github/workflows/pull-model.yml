name: pull-ollama-llm
on: 
  workflow_dispatch:
    inputs:
      tags:
        description: 'tags to create build'
jobs:
  summarize:
    runs-on: ubuntu-latest
    permissions: write-all
    steps:
    - name: Delete huge unnecessary tools folder
      run: rm -rf /opt/hostedtoolcache    
    - uses: actions/checkout@v3
    - name: download
      run: |
        pip install httpx rich
        python ollama-dl.py qwen3:30b-a3b -d models
    - name: create release and upload assets
      uses: softprops/action-gh-release@v2
      with:
        files: |
          models/blobs/sha256-e9183b5c18a0cf736578c1e3d1cbd4b7e98e3ad3be6176b68c20f156d54a07ac.017
          models/blobs/sha256-e9183b5c18a0cf736578c1e3d1cbd4b7e98e3ad3be6176b68c20f156d54a07ac.032
        tag_name: ${{ github.event.inputs.tags }}
      

      
